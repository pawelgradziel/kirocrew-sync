# Test-only dependencies for app/tests/.
#
# Kept separate from requirements.txt (the production/install-script
# dependency list, see docs/kirocrew-app-plan.md 5.1) so that installing the
# shipped app never pulls in test tooling. Install with:
#   pip install -r app/requirements.txt -r app/requirements-dev.txt

-r requirements.txt

pytest>=7.4.0
httpx>=0.24.0  # starlette TestClient, used by tests/test_server.py
