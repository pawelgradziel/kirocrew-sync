# Working conventions

- Work directly on `master` (this repo's default branch). Do not create
  feature branches or pull requests for normal changes — commit and push
  straight to `master`.
- This is a single-maintainer repo; PRs add no review value here.
